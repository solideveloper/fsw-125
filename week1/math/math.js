const sum = (a, b) => {
     return a + b
}

const subtract = (c, d) => {
     return c - d
}

const divide = (e, f) => {
     return e / f
}

const product = (g, h) => {
     return g * h
}
module.exports = {
     sum,
     subtract,
     divide,
     product
};